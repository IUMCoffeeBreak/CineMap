import { createSwitchNavigator } from "react-navigation";

export const constants = {
    tabs: {
        HOME: "Home",
        SETTINGS: "Settings",
        MAP: "Map",
    },
};

export default constants;
